redis-ha:
  enabled: false

controller:
  replicas: 1

server:
  replicas: 1
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - argocd.localhost

repoServer:
  replicas: 1

applicationSet:
  replicaCount: 1

dex:
  enabled: false

notifications:
  enabled: false

configs:

  params:
     server.insecure: true
